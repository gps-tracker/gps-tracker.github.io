<div class="section  indigo  lighten-5">
	<div class="row">
		{{!titulo}}
		<h2 class="center">Otras funcionalidades de GPS tracker</h2>
		{{!seccion para pantallas muy grandes y muy pequeñas}}
		<div class="container">
			{{#each info}}
			<div class="row no-mar">
				<div class="col s12 m3 l2 center">
					<img class="responsive-img otras-ventajas"  src="{{this.img}}" alt="{{this.titulo}}">
				</div>
				<div class="col s12 m9 l10">
					<p><span class="orange-text bold">{{this.titulo}}: </span> {{this.descripcion}}</p>
				</div>
			</div>
			{{/each}}
		</div>
	</div>
</div>